name: Update StreetScore Data

on:
  schedule:
    # Runs every day at 2:00 AM UTC
    - cron: "0 2 * * *"

  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    env:
      USE_DATABASE: shared
      DATABASE_URL: ${{ secrets.NEON_SHARED_DATABASE_URL }}
      NEON_SHARED_DATABASE_URL: ${{ secrets.NEON_SHARED_DATABASE_URL }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install Dependencies
        run: npm ci

      - name: Fetch Latest Data
        run: npm run fetch

      - name: Import Data
        run: npm run db:import

      - name: Generate Rankings
        run: npm run db:rankings
